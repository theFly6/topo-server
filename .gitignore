# -------------------------------------------------------------
# Node.js / npm 
# -------------------------------------------------------------

# 1. 依赖目录
# 忽略所有 Node.js 模块安装的文件夹
node_modules
node_modules/
# 忽略 npm 缓存或调试日志
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
# 忽略锁文件，除非您决定在仓库中保留它们（通常建议保留 package-lock.json）
# package-lock.json
# yarn.lock 
# pnpm-lock.yaml

# -------------------------------------------------------------
# 2. 构建/编译产物
# -------------------------------------------------------------

# 忽略构建输出目录 (例如：Webpack, Babel, TypeScript 编译输出)
dist
build
/out
/release
/tmp

# TypeScript 缓存文件
*.tsbuildinfo

# -------------------------------------------------------------
# 3. 运行时/IDE/操作系统
# -------------------------------------------------------------

# V8 编译缓存
.nyc_output
coverage

# IDE 和编辑器配置 (VS Code)
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
.history

# 操作系统临时文件
.DS_Store
Thumbs.db

# -------------------------------------------------------------
# 4. 敏感信息 / 环境配置
# -------------------------------------------------------------

# 忽略环境配置文件
# **重要：这些文件包含敏感配置，绝不应该提交到 Git 仓库**
.env
.env.*
.env.local
.env.development.local
.env.test.local
.env.production.local

# 密钥或配置文件
config/*.local.json
*.key
*.pem

# -------------------------------------------------------------
# 5. 测试和调试 (可选)
# -------------------------------------------------------------

# 忽略 MoCA 临时文件
mochawesome-report
# 运行时产物
logs/
pid/
workers copy.conf
